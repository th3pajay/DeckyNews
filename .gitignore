lib-cov
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz
*.swp

pids
logs
results
tmp

# Coverage reports
coverage

# API keys and secrets
.env

# Dependency directory
node_modules
bower_components
.pnpm-store

# Ignore built ts files
dist/

__pycache__/

/.yalc
yalc.lock

# Ignore output folder
backend/out

# Make sure to ignore any instance of the loader's decky_plugin.py
decky_plugin.py

# Ignore decky CLI for building plugins
out
out/*
cli/
cli/*
cli/decky

# Build artifacts
*.zip
DeckyNews.zip

# Runtime database (written at plugin startup, never commit)
defaults/telemetry.db

# Temporary folders
temp_*
test_*

# Python wheels (downloaded as needed)
*.whl

# Bundled dependencies (generated during build)
py_modules/

# LLM binary (too large for git, downloaded separately)
bin/llamafile
bin/llamafile-*
bin/*.so

# Legacy LLM binary (superseded by llamafile)
bin/llama-cli

# Versioned library files (not matched by bin/*.so)
bin/lib*
